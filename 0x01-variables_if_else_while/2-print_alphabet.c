#include <stdlib.h>
#include <stdio.h>


/**
 * main - this is the main function
 * Return: a return value of 0 if no error
 */
int main(void)
{
	char alpha = 'a';

	for (; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
	}
	putchar('\n')
	return (0);
}
