#include <stdlib.h>
#include <time.h>
#include <stdio.h>


/**
 * main - this is the main function
 * Return: a return value of 0 if no error
 */
int main(void)
{
	char alphaL = 'a';

	for (; alphaL <= 'z'; alphaL++)
	{
		if ((alphaU == 'q') || (alphaU == 'e'))
		{
			alphaL++;
		}
		putchar(alphaL);
	}
	putchar('\n');
	return (0);
}
